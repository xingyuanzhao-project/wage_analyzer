# Hugo build output and caches
/public/
/resources/_gen/
.hugo_build.lock

# Node (only used transiently, never committed)
node_modules/

# Python
__pycache__/
*.pyc

# Local Hugo binary (installed outside repo, but guard anyway)
/hugo-bin/
hugo-server.log

# OS cruft
.DS_Store
Thumbs.db
